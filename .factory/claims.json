[
  {
    "id": "identity-consistency",
    "claim": "Checks version, build, and bundle ID consistency",
    "where": "landing procedure, README",
    "test": "cargo test claim_identity_consistency",
    "sandbox": "bundled sample with three seeded identity mismatches"
  },
  {
    "id": "release-completeness",
    "claim": "Checks localized metadata, accepted App Store screenshot device sets and pixel sizes, privacy declarations, and reason codes",
    "where": "landing, README",
    "test": "cargo test claim_release_completeness",
    "sandbox": "bundled sample with seeded metadata/privacy inconsistencies, invalid sibling and Team-added reason codes, an unknown App Store locale, truncated JPEG/PNG stubs, a decodable 1×1 JPEG, and an unknown device-set key"
  },
  {
    "id": "sample-screenshot-dimensions",
    "claim": "The bundled iphone-69 sample is exactly 1320×2868 pixels",
    "where": "README screenshot guidance",
    "test": "cargo test claim_sample_screenshot_dimensions_match_the_documented_fixture",
    "sandbox": "decode the shipped JPEG and assert its exact width and height"
  },
  {
    "id": "markdown-packet",
    "claim": "Prints a dated Markdown review packet that names the rules version",
    "where": "landing, README",
    "test": "cargo test claim_markdown_packet",
    "sandbox": "temporary directory populated only with bundled sample data"
  },
  {
    "id": "core-without-team-license",
    "claim": "Core checks and packet export work without a Team license",
    "where": "landing facts, Team policy section, README",
    "test": "cargo test claim_core_gate_runs_without_team_license_and_writes_packet",
    "sandbox": "fresh CLI process with bundled sample inputs and no Team policy; assert all eight checks and a packet"
  },
  {
    "id": "bundled-cli-demo",
    "claim": "The CLI demo runs on bundled data in a new temporary workspace",
    "where": "README",
    "test": "cargo test claim_bundled_demo",
    "sandbox": "fresh command process; assert packet exists under the reported temp path"
  },
  {
    "id": "archive-inspection",
    "claim": "Extracts identity and privacy declarations from .xcarchive directories and .ipa files, then checks the release",
    "where": "landing procedure, install command, README",
    "test": "cargo test claim_archive_inspection_extracts_xcarchive_and_ipa_then_checks_release",
    "sandbox": "shipped Harbor Log .xcarchive fixture plus an IPA assembled from the same property lists in a temporary directory; assert extracted JSON and PASS packet"
  },
  {
    "id": "same-checker-demo",
    "claim": "The website sample and command use the same bundled checker",
    "where": "landing preview, demo page",
    "test": "cargo test claim_demo_recording_matches_bundled_cli",
    "sandbox": "fresh CLI demo; assert copied bundled inputs, matching CLI result, and website recording command/status markers"
  },
  {
    "id": "actionable-mismatch-errors",
    "claim": "Errors name the mismatch and the next fix",
    "where": "landing preview",
    "test": "cargo test claim_actionable_mismatch_error_names_values_and_fix",
    "sandbox": "fresh CLI process with a seeded version mismatch; assert the error reports both values and a repair instruction"
  },
  {
    "id": "queue-plan",
    "claim": "Queue dates account for an active submission and the configured buffer",
    "where": "landing packet preview, README",
    "test": "cargo test claim_queue_plan",
    "sandbox": "bundled sample with one active submission, two review days, and two buffer days"
  },
  {
    "id": "queue-input-validation",
    "claim": "Rejects incomplete or unknown queue entries before it can pass",
    "where": "README queue input",
    "test": "cargo test claim_queue_input_validation_rejects_incomplete_or_unknown_entries",
    "sandbox": "bundled sample with blank version/build, an unknown status, and an active submission dated after the intended release; assert HOLD findings and conservative queue count"
  },
  {
    "id": "queue-date-limits",
    "claim": "Rejects queue durations outside the calendar range without crashing",
    "where": "README queue input",
    "test": "cargo test claim_queue_date_limits_hold_without_panicking",
    "sandbox": "bundled sample with maximum and minimum i64 review and buffer durations; assert HOLD findings"
  },
  {
    "id": "cli-local",
    "claim": "The CLI has no telemetry or network client and reads only local inputs",
    "where": "landing privacy section, README, privacy page",
    "test": "cargo test claim_cli_local",
    "sandbox": "compiled CLI source and dependency manifest checked for network client markers"
  },
  {
    "id": "cli-exit-codes",
    "claim": "Uses exit code 0 for PASS, 2 for HOLD, and 1 for invalid input",
    "where": "README check a release",
    "test": "cargo test claim_cli_exit_codes",
    "sandbox": "fresh CLI processes with a bundled passing sample, a seeded mismatch, and a missing input file"
  },
  {
    "id": "cli-json-schema",
    "claim": "JSON output contains passed, summary, findings, queue, and packet_path",
    "where": "README check a release",
    "test": "cargo test claim_cli_json_schema",
    "sandbox": "fresh CLI process with bundled sample inputs; parse JSON and assert every documented field"
  },
  {
    "id": "one-click-demo",
    "claim": "The site opens a complete sample release in one click",
    "where": "landing primary action",
    "test": "npm run verify:browser-claim -- --grep @claim:one-click-demo",
    "sandbox": "fresh browser context at the home page"
  },
  {
    "id": "browser-demo-local",
    "claim": "The browser demo saves no personal release data and sends no cross-origin requests",
    "where": "demo banner and privacy page",
    "test": "npm run verify:browser-claim -- --grep @claim:browser-demo-local",
    "sandbox": "fresh browser context seeded with unrelated real release, license, and draft keys; reset demo; assert those values stay byte-for-byte unchanged and every request stays same-origin"
  },
  {
    "id": "offline-shell",
    "claim": "The demo works offline after one visit, and its offline shell caches same-origin static files only",
    "where": "landing facts, privacy page",
    "test": "npm run verify:browser-claim -- --grep @claim:offline-shell",
    "sandbox": "fresh browser context at ?demo=1; list every cached response, assert allowed same-origin static paths, switch the context offline, and reload the sample"
  },
  {
    "id": "rust-msrv",
    "claim": "Builds with Rust 1.85 or newer",
    "where": "README",
    "test": "cargo +1.85.0 test --all-targets --locked",
    "sandbox": "clean Rust 1.85.0 toolchain and locked dependency graph"
  },
  {
    "id": "license-restore",
    "claim": "A buyer can paste, verify, and store a Team license",
    "where": "paid tier and privacy page",
    "test": "npm run verify:browser-claim -- --grep @claim:license-restore",
    "sandbox": "fresh browser context with a recorded valid Sociobot verification response"
  },
  {
    "id": "team-purchase",
    "claim": "A $39 one-time Team purchase starts at the hosted Sociobot checkout",
    "where": "paid tier, terms page, README",
    "test": "npm run verify:browser-claim -- --grep @claim:team-purchase",
    "sandbox": "fresh browser context with the checkout navigation intercepted; assert the exact price and production Sociobot URL"
  },
  {
    "id": "team-policy-download",
    "claim": "A verified Team license enables a local policy download with the chosen queue limit and Apple reason codes",
    "where": "paid tier, terms page",
    "test": "npm run verify:browser-claim -- --grep @claim:team-policy-download",
    "sandbox": "fresh browser context with a cached recorded license verdict; choose queue and reason settings, inspect the YAML, then pass it to the real CLI"
  },
  {
    "id": "team-queue-history",
    "claim": "A Team policy supports queue histories beyond three submissions",
    "where": "paid tier, terms page, README",
    "test": "cargo test claim_team_policy_supports_queue_history_beyond_three_submissions",
    "sandbox": "bundled sample with five completed submissions; assert the default limit warns and a Team policy of eight removes that warning"
  },
  {
    "id": "license-metadata",
    "claim": "The CLI is licensed under MIT",
    "where": "terms page, README, package metadata",
    "test": "node --test --test-name-pattern='@claim:license-metadata' tests/site.test.js",
    "sandbox": "compare the LICENSE text, Cargo package license, README license section, and terms copy"
  },
  {
    "id": "version-metadata",
    "claim": "The site footer identifies the CLI version and deployed source build",
    "where": "footer on every route, including 404",
    "test": "npm run verify:browser-claim -- --grep @claim:version-metadata",
    "sandbox": "compare Cargo and package versions with CLI --version, then compare the generated Git build id on the SPA and standalone 404"
  }
]
